# Repository Agent Rules

이 파일은 이 저장소에서 작업하는 모든 코딩 에이전트에 적용된다.
하위 디렉터리에 별도의 `AGENTS.md`가 있으면 더 구체적인 하위 규칙을 우선한다.

## 프로젝트 목적

- Vue 3 + TypeScript로 작성된 개인 이력서 웹사이트다.
- `src/data/resumeData.ts`가 사실 정보의 원본이다.
- `agent/resume-agent.ts`는 채용공고에 맞춰 원본의 순서와 표현을 조정한다.
- `src/data/activeResume.ts`는 에이전트가 생성하는 적용 결과이므로 직접 편집하지 않는다.

## 필수 명령어

- 의존성 설치: `npm ci`
- 개발 서버: `npm run dev`
- 에이전트 검증: `npm run agent:check`
- 테스트: `npm test`
- 전체 품질 검사: `npm run check`
- 프로덕션 빌드: `npm run build`

코드나 데이터를 변경한 뒤에는 최소한 관련 테스트를 실행한다. 병합 가능한 변경은 `npm run check`를 통과해야 한다.

## 구현 규칙

- TypeScript strict 모드를 유지하고 `any` 사용을 피한다.
- 기존 Vue Composition API와 `<script setup>` 패턴을 따른다.
- 사용자에게 보이는 문구는 현재 문서의 언어와 어조를 유지한다.
- 새로운 패키지는 표준 기능으로 해결하기 어려울 때만 추가한다.
- `node_modules/`와 `dist/`를 직접 수정하거나 커밋하지 않는다.
- 관련 없는 사용자 변경을 되돌리거나 파일 전체를 불필요하게 재포맷하지 않는다.

## 이력서 데이터 안전 규칙

- 없는 경력, 기술, 수치, 수상 또는 활동을 만들지 않는다.
- 회사명이나 채용공고의 표현을 맞추기 위해 사실을 과장하지 않는다.
- 프로젝트의 식별 정보와 원본 기술 목록은 유지한다.
- 프로젝트 순서, 프로젝트 내부 섹션 선택, 스킬 순서, 사실을 유지한 bullet 표현만 조정할 수 있다.
- 프로젝트마다 원본에 존재하는 섹션을 최소 2개 유지한다.
- 생성된 variant는 저장하거나 적용하기 전에 반드시 검증기를 통과해야 한다.
- 실제 이력서 사실을 수정할 때는 먼저 `src/data/resumeData.ts`를 변경하고 변경 근거를 PR에 적는다.

## 보안 규칙

- API 키, 토큰, 개인 인증정보와 `.env` 파일을 커밋하지 않는다.
- 로그와 오류 메시지에 비밀 값을 출력하지 않는다.
- 외부 채용공고 HTML은 신뢰하지 않는 입력으로 취급한다.
- 외부 입력이 로컬 경로나 실행 명령을 결정하게 만들지 않는다.

## Git 규칙

- 파일을 수정하기 전에 `git branch --show-current`로 현재 브랜치를 확인한다.
- `main` 또는 `master`에 있다면 반드시 먼저 `npm run branch -- <type> <topic>`으로 새 작업 브랜치를 만든다.
- 하나의 브랜치는 하나의 작업 목적만 다룬다. 서로 다른 요청은 별도 브랜치로 분리한다.
- 기본 브랜치에 미커밋 변경이 있다면 새 변경을 시작하지 말고 기존 변경을 전용 브랜치로 먼저 옮긴다.
- 브랜치 이름은 `feat/`, `fix/`, `docs/`, `refactor/`, `test/`, `chore/` 중 하나로 시작한다.
- 커밋은 하나의 의도만 담고 Conventional Commits 형식을 사용한다.
- 예: `feat(agent): add resume variant validation`
- 커밋 전에 `git diff --check`와 `npm run check`를 실행한다.
- 공유 브랜치에 force push하거나 검증을 우회하지 않는다.
- PR 브랜치 이름과 제목은 GitHub Actions 정책 검사를 통과해야 한다.
- 자세한 협업 절차는 `CONTRIBUTING.md`를 따른다.

## 완료 기준

- 요청된 동작이 구현되어 있다.
- 테스트와 빌드가 통과한다.
- 에이전트 또는 데이터 변경은 검증 하네스를 통과한다.
- 사용법이나 명령이 바뀌면 README도 함께 갱신한다.
